\# Build Log



\## Setup

\- Gemini test failed with 404: `gemini-2.5-flash` is no longer available to new users. The error suggested `gemini-3.6-flash`; switched `VISION\_MODEL` in `.env` and `.env.example`. Model names live in env so no code change was needed.

\- Docker CLI couldn't connect until Docker Desktop's engine was started.

\- Moved the project out of OneDrive (Desktop path) to `%USERPROFILE%\\projects` to avoid syncing node\_modules.

